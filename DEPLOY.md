## Vercel deployment

- Upload/push this project to GitHub.
- Import the repository into Vercel.
- Build command: none required for this simple serverless/static project.
- Add `GEMINI_API_KEY` under Environment Variables.
- Optional `GEMINI_MODEL`.
- Deploy.
- Test `/` and then run a listing with an image.

Do not put the key in `index.html`.
